# Core (mirrors [tool.poetry.dependencies])
numpy>=1.26.4
scipy>=1.12.0
matplotlib>=3.8.3

# Optional extras (mirror [tool.poetry.extras]); install with
# pip install ".[torch]" and/or ".[gui]", or uncomment here:
# torch>=2.2.1
# PyQt6>=6.6.1

# Dev tooling (mirrors [tool.poetry.group.dev.dependencies])
pytest>=8.0.2
pytest-cov>=4.1.0
ruff>=0.15.21
mypy>=1.8.0